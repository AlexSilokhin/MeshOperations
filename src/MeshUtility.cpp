#include "MeshUtility.h"

// TODO: Implement functions below using parallel computation technics,
// e.g. multithreading, CUDA stuff

float MeshVolume(const Mesh& mesh)
{
	return 0.0f;
}

bool IsPointInsideMesh(const Mesh& mesh)
{
	return false;
}

bool IsMeshWatertight(const Mesh& mesh)
{
	return false;
}
